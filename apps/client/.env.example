# API base URL. For physical devices, use your Mac's LAN IP (see API server log on startup).
# Simulator/Emulator can use localhost.
EXPO_PUBLIC_API_URL=http://localhost:4000/api
# If using localhost above, the app replaces it with this IP on device. Set to your Mac's IP.
# EXPO_PUBLIC_NETWORK_IP=192.168.1.73
# Production:
# EXPO_PUBLIC_API_URL=https://api.poolcare.africa/api
